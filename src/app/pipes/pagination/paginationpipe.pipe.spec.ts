import { PaginationpipePipe } from './paginationpipe.pipe';

describe('PaginationpipePipe', () => {
  it('create an instance', () => {
    const pipe = new PaginationpipePipe();
    expect(pipe).toBeTruthy();
  });
});
